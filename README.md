# OPC_API
requirements : 
node 15.8 
OPC Database access (will be released after)

Install :
git clone
npm install
echo >> .env

edit .env like this : 
DB_Password=Yourpassword
DB_User=YourUser
DB_Host=YourHost
DB_Name=DatabaseName

